var ApiXhrTest = React.createClass({

    componentDidMount : function() {

    },
    render : function() {
        return (
            <div className={"page "+loc[32].pageName+" "+this.props.position}>
            </div>
        )
    }
});